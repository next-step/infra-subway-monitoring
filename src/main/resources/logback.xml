<?xml version="1.0" encoding="UTF-8"?>

<configuration debug="false">

    <springProfile name="prod">
        <property name="home" value="/home/ubuntu/nextstep/log/subway/" />
    </springProfile>

    <!--spring boot의 기본 logback base.xml은 그대로 가져간다.-->
    <include resource="org/springframework/boot/logging/logback/base.xml" />
    <include resource="logging/console-appender.xml" />
    <include resource="logging/file-appender.xml" />
    <include resource="logging/json-appender.xml" />

    <logger name="console" level="TRACE" >
        <appender-ref ref="console" />
    </logger>

    <logger name="file" level="INFO" >
        <appender-ref ref="file" />
    </logger>

    <logger name="json" level="INFO" >
        <appender-ref ref="json" />
    </logger>

</configuration>
