<?xml version="1.0" encoding="UTF-8"?>

<included>
    <if condition='System.getProperty("os.name").toLowerCase().contains("window")'>
        <then>
            <property name="home" value="C:/log/"/>    <!-- 그 외 os의 로그 경로 -->
        </then>
        <else>
            <property name="home" value="/log/infra-monitoring/"/>  <!-- 리눅스일 경우 로그 경로 -->
        </else>
    </if>

    <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${home}subway.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${home}subway-%d{yyyyMMdd}-%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>15MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
        <encoder>
            <charset>utf8</charset>
            <Pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} %thread %-5level %logger - %m%n
            </Pattern>
        </encoder>
    </appender>
</included>
