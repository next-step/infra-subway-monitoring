spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.38.52:3306/subway?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: masterpw
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.type.descriptor.sql: info

cloud:
  aws:
    stack:
      auto: false  # 로컬에서 실행시 AWS stack autoconfiguration 수행과정에서 발생하는 에러 방지
    region:
      static: ap-northeast-2
management:
  metrics:
    export:
      cloudwatch:
        namespace: tj3828-subway  # 해당 namespace로 Cloudwatch 메트릭을 조회 가능
        batch-size: 20
  endpoints:
    web:
      exposure:
        include: "*"
